import { Satellite } from './satellite';

describe('Satellite', () => {
  it('should create an instance', () => {
    expect(new Satellite()).toBeTruthy();
  });

  it('should create an instance', () => {
	expect(new Satellite()).toBeTruthy();
 });
 
});
